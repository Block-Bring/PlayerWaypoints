# PlayerWaypoints

## Commit 规范

本项目遵循 `.claude/commit-agent.md` 定义的 commit 规范。

当我说"提交"或"commit"时，请按以下流程处理：
  1. `git status` 查看变更
  2. `git add -A` 暂存
  3. 根据变更内容撰写 `type: description` 格式的 commit 消息
  4. 提交并询问是否推送
